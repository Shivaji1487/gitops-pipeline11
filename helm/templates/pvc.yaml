apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pipeline11java-pvc
  namespace: bank-production-v11
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: pipeline11java-pv
  storageClassName: manual