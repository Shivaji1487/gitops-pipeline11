apiVersion: v1
kind: Service
metadata:
  name: pipeline11java-service
  namespace: bank-production-v11
spec:
  type: ClusterIP
  selector:
    app: pipeline11java-app
  ports:
    - port: 80
      targetPort: 80 # Nginx container port